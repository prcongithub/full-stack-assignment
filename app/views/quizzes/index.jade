extends ../layouts/default

block main
  h1= title

block content
  - if (quizzes.length)
    - each quiz in quizzes
      include quiz

    - if (pages > 1)
      ul.pagination
        != createPagination(pages, page)
  - else
    h4.muted No quizzes.&nbsp;
      a(href="/quizzes/new") create one
